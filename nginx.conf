worker_processes 1;

events {
    worker_connections 1024;
}

http {

    resolver 127.0.0.11 valid=10s ipv6=off;

    server {
        listen 8080;

        location /api/auth {
            if ($request_method = 'OPTIONS') {
                include cors-headers.conf;
                add_header Content-Length 0;
                return 204;
            }

            include cors-headers.conf;

            set $upstream_user http://user-service:8081;
            proxy_pass $upstream_user;
            proxy_set_header Host $host;
            proxy_set_header Authorization $http_authorization;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        location /api/users {
            if ($request_method = 'OPTIONS') {
                include cors-headers.conf;
                add_header Content-Length 0;
                return 204;
            }

            include cors-headers.conf;

            set $upstream_user http://user-service:8081;
            proxy_pass $upstream_user;
            proxy_set_header Host $host;
            proxy_set_header Authorization $http_authorization;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        location /api/products {
            if ($request_method = 'OPTIONS') {
                include cors-headers.conf;
                add_header Content-Length 0;
                return 204;
            }

            include cors-headers.conf;

            set $upstream_catalog http://catalog-service:8082;
            proxy_pass $upstream_catalog;
            proxy_set_header Host $host;
            proxy_set_header Authorization $http_authorization;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        location /api/categories {
            if ($request_method = 'OPTIONS') {
                include cors-headers.conf;
                add_header Content-Length 0;
                return 204;
            }

            include cors-headers.conf;

            set $upstream_catalog http://catalog-service:8082;
            proxy_pass $upstream_catalog;
            proxy_set_header Host $host;
            proxy_set_header Authorization $http_authorization;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        location /api/carts {
            if ($request_method = 'OPTIONS') {
                include cors-headers.conf;
                add_header Content-Length 0;
                return 204;
            }

            include cors-headers.conf;

            set $upstream_cart http://cart-service:8083;
            proxy_pass $upstream_cart;
            proxy_set_header Host $host;
            proxy_set_header Authorization $http_authorization;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        location /api/orders {
            if ($request_method = 'OPTIONS') {
                include cors-headers.conf;
                add_header Content-Length 0;
                return 204;
            }

            include cors-headers.conf;

            set $upstream_order http://order-service:8084;
            proxy_pass $upstream_order;
            proxy_set_header Host $host;
            proxy_set_header Authorization $http_authorization;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        location /api/inventory {
            if ($request_method = 'OPTIONS') {
                include cors-headers.conf;
                add_header Content-Length 0;
                return 204;
            }

            include cors-headers.conf;

            set $upstream_inventory http://inventory-service:8085;
            proxy_pass $upstream_inventory;
            proxy_set_header Host $host;
            proxy_set_header Authorization $http_authorization;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }
}
